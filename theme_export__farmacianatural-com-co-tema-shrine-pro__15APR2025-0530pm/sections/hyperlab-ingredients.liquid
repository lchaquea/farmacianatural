{%- style -%}
  .hyperlab-ingredients {
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
    background-color: {{ section.settings.background_color }};
  }

  .hyperlab-ingredients__container {
    display: flex;
    align-items: center;
    gap: 4rem;
  }

  .hyperlab-ingredients__content {
    flex: 1;
  }

  .hyperlab-ingredients__image {
    flex: 1;
  }

  .hyperlab-ingredients__heading {
    margin-bottom: 2rem;
  }

  .hyperlab-ingredients__list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .hyperlab-ingredients__item {
    display: flex;
    align-items: center;
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }

  .hyperlab-ingredients__icon {
    margin-right: 1rem;
    font-size: 1.5rem;
  }

  @media screen and (max-width: 749px) {
    .hyperlab-ingredients {
      padding: {{ section.settings.mobile_padding_top }}px 0 {{ section.settings.mobile_padding_bottom }}px;
    }

    .hyperlab-ingredients__container {
      flex-direction: column;
      gap: 2rem;
    }

    .hyperlab-ingredients__image {
      order: -1;
    }
  }
{%- endstyle -%}

<div class="hyperlab-ingredients">
  <div class="page-width">
    <div class="hyperlab-ingredients__container">
      <div class="hyperlab-ingredients__content">
        {%- if section.settings.heading != blank -%}
          <h2 class="hyperlab-ingredients__heading">{{ section.settings.heading }}</h2>
        {%- endif -%}

        <ul class="hyperlab-ingredients__list">
          {%- for block in section.blocks -%}
            <li class="hyperlab-ingredients__item" {{ block.shopify_attributes }}>
              {%- if block.settings.icon != blank -%}
                <span class="hyperlab-ingredients__icon">{{ block.settings.icon }}</span>
              {%- endif -%}
              {{ block.settings.ingredient }}
            </li>
          {%- endfor -%}
        </ul>
      </div>

      {%- if section.settings.image != blank -%}
        <div class="hyperlab-ingredients__image">
          {{ section.settings.image | image_url: width: 800 | image_tag:
            loading: 'lazy',
            width: section.settings.image.width,
            height: section.settings.image.height,
            class: 'hyperlab-ingredients__image-element'
          }}
        </div>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hyperlab Ingredients",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Ingredientes Naturales"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Ingredients Image"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Mobile Padding Top",
      "default": 24
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Mobile Padding Bottom",
      "default": 24
    }
  ],
  "blocks": [
    {
      "type": "ingredient",
      "name": "Ingredient",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (Emoji)",
          "default": "🌿"
        },
        {
          "type": "text",
          "id": "ingredient",
          "label": "Ingredient",
          "default": "Agua Purificada"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hyperlab Ingredients",
      "blocks": [
        {
          "type": "ingredient",
          "settings": {
            "icon": "🌿",
            "ingredient": "Agua Purificada"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "icon": "🍯",
            "ingredient": "Miel de abejas"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "icon": "🌰",
            "ingredient": "Jugo de Chontaduro"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "icon": "🍇",
            "ingredient": "Jugo de Borojo"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "icon": "💊",
            "ingredient": "Vitaminas C, B3, E, B6, B2, B1, D"
          }
        }
      ]
    }
  ]
}
{% endschema %} 