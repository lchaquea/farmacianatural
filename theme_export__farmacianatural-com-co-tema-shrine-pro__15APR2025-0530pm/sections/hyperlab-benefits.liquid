{%- style -%}
  .hyperlab-benefits {
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
    background-color: {{ section.settings.background_color }};
  }

  .hyperlab-benefits__heading {
    text-align: {{ section.settings.heading_alignment }};
    margin-bottom: 2rem;
  }

  .hyperlab-benefits__grid {
    display: grid;
    grid-template-columns: repeat({{ section.settings.columns_desktop }}, 1fr);
    gap: 2rem;
  }

  .hyperlab-benefits__item {
    text-align: center;
    padding: 2rem;
    background: {{ section.settings.card_background_color }};
    border-radius: 8px;
  }

  .hyperlab-benefits__icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .hyperlab-benefits__title {
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }

  .hyperlab-benefits__description {
    font-size: 1rem;
    line-height: 1.5;
  }

  @media screen and (max-width: 749px) {
    .hyperlab-benefits {
      padding: {{ section.settings.mobile_padding_top }}px 0 {{ section.settings.mobile_padding_bottom }}px;
    }

    .hyperlab-benefits__grid {
      grid-template-columns: repeat({{ section.settings.columns_mobile }}, 1fr);
    }
  }
{%- endstyle -%}

<div class="hyperlab-benefits">
  <div class="page-width">
    {%- if section.settings.heading != blank -%}
      <h2 class="hyperlab-benefits__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}

    <div class="hyperlab-benefits__grid">
      {%- for block in section.blocks -%}
        <div class="hyperlab-benefits__item" {{ block.shopify_attributes }}>
          {%- if block.settings.icon != blank -%}
            <div class="hyperlab-benefits__icon">{{ block.settings.icon }}</div>
          {%- endif -%}
          {%- if block.settings.title != blank -%}
            <h3 class="hyperlab-benefits__title">{{ block.settings.title }}</h3>
          {%- endif -%}
          {%- if block.settings.description != blank -%}
            <div class="hyperlab-benefits__description">{{ block.settings.description }}</div>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hyperlab Benefits",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Beneficios del V10"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Heading Alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 4,
      "step": 1,
      "label": "Columns on Desktop",
      "default": 4
    },
    {
      "type": "range",
      "id": "columns_mobile",
      "min": 1,
      "max": 2,
      "step": 1,
      "label": "Columns on Mobile",
      "default": 2
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "card_background_color",
      "label": "Card Background Color",
      "default": "#F8F8F8"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Mobile Padding Top",
      "default": 24
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Mobile Padding Bottom",
      "default": 24
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (Emoji)",
          "default": "🍽️"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Mejora el apetito"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Mejora el apetito de los niños entre 1 año y 16 años</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hyperlab Benefits",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "icon": "🍽️",
            "title": "Mejora el apetito",
            "description": "<p>Mejora el apetito de los niños entre 1 año y 16 años</p>"
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "🛡️",
            "title": "Sistema inmunológico",
            "description": "<p>Mejora el sistema inmunológico y respiratorio</p>"
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "🍊",
            "title": "Efectivo contra la gripa",
            "description": "<p>Efectivo para evitar la gripa recurrente</p>"
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "💪",
            "title": "Vitaminas esenciales",
            "description": "<p>Contiene vitamina C, B3, E, B6, B2, B1, D</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %} 