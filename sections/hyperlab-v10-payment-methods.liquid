{%- assign product = all_products[section.settings.product_handle] | default: product -%}

<div class="hyperlab-v10-payment-methods">
  <h2 class="section-title">{{ section.settings.title }}</h2>

  <div class="payment-methods-container">
    <div class="payment-cards">
      {% for block in section.blocks %}
        <a class="payment-card" href="{{ block.settings.link }}" target="_blank" rel="noopener" {{ block.shopify_attributes }}>
          {% if block.settings.logo != blank %}
            <img src="{{ block.settings.logo | img_url: '300x' }}" alt="{{ block.settings.name }} logo" class="payment-logo" loading="lazy">
          {% endif %}
        </a>
      {% endfor %}
    </div>
    <p class="payment-note">{{ section.settings.note }}</p>
  </div>
</div>

{% schema %}
{
  "name": "V10 Payment Methods",
  "settings": [
    {
      "type": "product",
      "id": "product_handle",
      "label": "Product"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Ordena ya con envío gratis"
    },
    {
      "type": "richtext",
      "id": "note",
      "label": "Payment Note",
      "default": "<p><strong><em>Método de pago a cuotas</em></strong></p>"
    }
  ],
  "blocks": [
    {
      "type": "payment_method",
      "name": "Payment Method",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "Payment Method Name",
          "default": "Addi"
        },
        {
          "type": "image_picker",
          "id": "logo",
          "label": "Logo"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "V10 Payment Methods",
      "blocks": [
        {
          "type": "payment_method",
          "settings": {
            "name": "Addi",
            "logo": "",
            "link": ""
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "name": "Sistecrédito",
            "logo": "",
            "link": ""
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "name": "MercadoPago",
            "logo": "",
            "link": ""
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "name": "PSE",
            "logo": "",
            "link": ""
          }
        }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
.hyperlab-v10-payment-methods {
  padding: calc(var(--spacing-xl) / 8) 0;
  text-align: center;
}

.section-title {
  font-size: var(--font-size-xl);
  font-weight: bold;
  margin-bottom: var(--spacing-lg);
  text-transform: uppercase;
}

.payment-methods-container {
  max-width: 800px;
  margin: 0 auto;
  padding: 0 var(--spacing-md);
}

.payment-cards {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 9px;
  margin: 8px 0 6px 0;
  justify-items: center;
  align-items: stretch;
}

.payment-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: #fff;
  border-radius: 22px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.07);
  padding: 18px 8px 12px 8px;
  min-height: 120px;
  min-width: 0;
  text-decoration: none !important;
  border: 1.5px solid #f3f3f3;
  transition: box-shadow 0.18s, transform 0.18s, border 0.18s;
  cursor: pointer;
  position: relative;
  width: 100%;
  max-width: 140px;
  height: 140px;
}

.payment-card:hover, .payment-card:focus {
  box-shadow: 0 8px 32px rgba(255,107,0,0.13);
  border: 1.5px solid #ffb74d;
  transform: translateY(-2px) scale(1.03);
}

.payment-logo {
  width: 100%;
  max-width: 110px;
  max-height: 80px;
  object-fit: contain;
  margin: 0 auto;
  background: transparent;
  display: block;
}

.payment-name {
  font-size: 1.08rem;
  font-weight: 600;
  color: #1a1a1a;
  text-align: center;
  margin-top: 2px;
  letter-spacing: 0.01em;
}

.payment-note {
  font-size: 1.08rem;
  color: #666;
  margin: 0;
  font-weight: 400;
  text-align: center;
}

.payment-note strong { font-weight: 700; }
.payment-note em { font-style: italic; }

@media screen and (max-width: 767px) {
  .payment-cards {
    grid-template-columns: repeat(4, 1fr);
    gap: 8px;
    margin: 6px 0 4px 0;
  }
  .payment-card {
    padding: 10px 2px 8px 2px;
    min-width: 0;
    min-height: 80px;
    max-width: 80px;
    height: 80px;
  }
  .payment-logo {
    max-width: 60px;
    max-height: 48px;
  }
  .payment-name {
    font-size: 0.82rem;
  }
  .payment-note {
    font-size: 0.92rem;
  }
}
{% endstylesheet %} 