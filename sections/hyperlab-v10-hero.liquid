{%- assign product = all_products[section.settings.product_handle] | default: product -%}
<!-- Updated: May 1, 2024 -->

<div class="hyperlab-v10-hero {% if section.settings.full_width %}full-width{% endif %}">
  <div class="hero-content {% if section.settings.full_width %}page-width{% endif %}">
    <div class="hero-header">
      <div class="logo-container">
        {%- if section.settings.logo != blank -%}
          <img src="{{ section.settings.logo | img_url: '100x' }}" alt="{{ shop.name }}" class="hero-logo">
        {%- endif -%}
      </div>
      
      <h1 class="hero-title">{{ section.settings.headline | default: "¿TUS HIJOS NO ESTÁN COMIENDO BIEN?" }}</h1>
    </div>

    <div class="product-gallery">
      <div class="main-image">
        {%- if section.settings.main_image != blank -%}
          <img src="{{ section.settings.main_image | img_url: 'large' }}" 
               alt="{{ section.settings.main_image.alt | escape }}"
               class="featured-image"
               id="ProductPhotoImg">
        {%- endif -%}
      </div>

      {%- assign gallery_images = section.blocks | where: "type", "gallery_image" -%}
      {%- if gallery_images.size > 0 -%}
        <div class="thumbnail-list">
          {%- for block in gallery_images -%}
            {%- if block.settings.image != blank -%}
              <button class="thumbnail-item" data-image-id="{{ block.settings.image.id }}">
                <img src="{{ block.settings.image | img_url: '100x100', crop: 'center' }}" 
                     alt="{{ block.settings.image.alt | escape }}"
                     data-full-url="{{ block.settings.image | img_url: 'large' }}"
                     class="thumbnail">
              </button>
            {%- endif -%}
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>

    <div class="hero-cta">
      <div class="subheadline-container">
        <h2 class="subheadline">{{ section.settings.subheadline | default: "¡NO TE PREOCUPES MÁS!" }}</h2>
        {%- if section.settings.certification_badge != blank -%}
          <div class="certification-badge">
            <img src="{{ section.settings.certification_badge | img_url: '100x' }}" 
                 alt="Certificación INVIMA" 
                 class="certification-image">
          </div>
        {%- endif -%}
      </div>
      <a href="{{ section.settings.button_link | default: '#' }}" class="primary-button {{ section.settings.button_animation }}">
        {{ section.settings.button_text | default: "CLICK AQUÍ Y PAGA AL RECIBIR" }}
        <span class="button-subtext">{{ section.settings.button_subtext | default: "paga en casa contraentrega" }}</span>
      </a>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "V10 Hero Section",
  "settings": [
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full Width Section",
      "default": false
    },
    {
      "type": "header",
      "content": "Content Settings"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Headline",
      "default": "¿TUS HIJOS NO ESTÁN COMIENDO BIEN?"
    },
    {
      "type": "text",
      "id": "subheadline",
      "label": "Subheadline",
      "default": "¡NO TE PREOCUPES MÁS!"
    },
    {
      "type": "image_picker",
      "id": "certification_badge",
      "label": "Certification Badge"
    },
    {
      "type": "header",
      "content": "Product Gallery"
    },
    {
      "type": "product",
      "id": "product_handle",
      "label": "Product"
    },
    {
      "type": "image_picker",
      "id": "main_image",
      "label": "Main Product Image"
    },
    {
      "type": "header",
      "content": "Button Settings"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "CLICK AQUÍ Y PAGA AL RECIBIR"
    },
    {
      "type": "text",
      "id": "button_subtext",
      "label": "Button Subtext",
      "default": "paga en casa contraentrega"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "select",
      "id": "button_animation",
      "label": "Button Animation",
      "options": [
        {
          "value": "shake",
          "label": "Shake"
        },
        {
          "value": "pulse",
          "label": "Pulse"
        },
        {
          "value": "bounce",
          "label": "Bounce"
        },
        {
          "value": "none",
          "label": "None"
        }
      ],
      "default": "shake"
    }
  ],
  "blocks": [
    {
      "type": "gallery_image",
      "name": "Gallery Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "max_blocks": 5
}
{% endschema %}

{% stylesheet %}
.hyperlab-v10-hero {
  padding: 60px 0;
  background-color: #fff;
}

.hyperlab-v10-hero.full-width {
  width: 100%;
  max-width: none;
  padding-left: 0;
  padding-right: 0;
}

.hero-content {
  margin: 0 auto;
  padding: 0 24px;
  max-width: 1200px;
}

.hero-header {
  text-align: center;
  margin-bottom: 40px;
}

.logo-container {
  margin-bottom: 24px;
}

.hero-logo {
  max-width: 120px;
  height: auto;
}

.hero-title {
  font-size: 32px;
  font-weight: 800;
  margin: 0;
  line-height: 1.2;
  color: #1a1a1a;
}

.product-gallery {
  margin-bottom: 40px;
  position: relative;
}

.main-image {
  margin-bottom: 24px;
  background: #ffffff;
  padding: 32px;
  border-radius: 12px;
  border: 1px solid #ff6b00;
  transition: all 0.3s ease;
}

.featured-image {
  width: 100%;
  height: auto;
  max-width: 500px;
  margin: 0 auto;
  display: block;
  transition: transform 0.3s ease;
}

.thumbnail-list {
  display: flex;
  gap: 16px;
  overflow-x: auto;
  padding: 8px 4px;
  justify-content: center;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
  -ms-overflow-style: none;
  background: transparent;
  max-width: 100%;
  margin: 0 auto;
  position: relative;
}

.thumbnail-list::-webkit-scrollbar {
  display: none;
}

/* Custom scrollbar arrows */
.thumbnail-list::before,
.thumbnail-list::after {
  content: '';
  position: absolute;
  top: 50%;
  width: 24px;
  height: 24px;
  background: #ff6b00;
  transform: translateY(-50%) rotate(45deg);
  cursor: pointer;
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: 2;
}

.thumbnail-list::before {
  left: 0;
  border-bottom: 2px solid #fff;
  border-left: 2px solid #fff;
}

.thumbnail-list::after {
  right: 0;
  border-top: 2px solid #fff;
  border-right: 2px solid #fff;
}

.thumbnail-list:hover::before,
.thumbnail-list:hover::after {
  opacity: 1;
}

.thumbnail-item {
  border: 1px solid transparent;
  padding: 4px;
  background: #ffffff;
  cursor: pointer;
  opacity: 0.6;
  transition: all 0.3s ease;
  flex: 0 0 auto;
  width: 80px;
  height: 80px;
  position: relative;
  overflow: hidden;
  border-radius: 8px;
}

.thumbnail-item:hover {
  opacity: 0.8;
  transform: translateY(-2px);
}

.thumbnail-item.active {
  opacity: 1;
  border-color: #ff6b00;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(255, 107, 0, 0.1);
}

.thumbnail {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 6px;
  display: block;
  background: #fff;
}

.hero-cta {
  text-align: center;
  margin-bottom: 40px;
}

.subheadline-container {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 15px;
  margin-bottom: 20px;
}

.certification-badge {
  flex-shrink: 0;
}

.certification-image {
  max-width: 60px;
  height: auto;
  display: block;
}

.primary-button {
  background-color: #ff6b00;
  color: white;
  border: none;
  padding: 15px 30px;
  border-radius: 25px;
  font-weight: bold;
  cursor: pointer;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
  max-width: 300px;
  margin: 0 auto;
  text-decoration: none;
  transition: transform 0.3s ease;
}

.button-subtext {
  font-size: 12px;
  opacity: 0.8;
}

/* Button Animations */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
  20%, 40%, 60%, 80% { transform: translateX(5px); }
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

.shake {
  animation: shake 1.5s ease-in-out infinite;
}

.pulse {
  animation: pulse 1.5s ease-in-out infinite;
}

.bounce {
  animation: bounce 1.5s ease-in-out infinite;
}

@media screen and (min-width: 768px) {
  .hero-title {
    font-size: 42px;
  }

  .primary-button {
    font-size: 18px;
  }

  .featured-image {
    max-width: 600px;
  }

  .thumbnail-item {
    width: 100px;
    height: 100px;
  }

  .hero-content {
    padding: 0 40px;
  }
}
{% endstylesheet %}

{% javascript %}
document.addEventListener('DOMContentLoaded', function() {
  const thumbnails = document.querySelectorAll('.thumbnail-item');
  const mainImage = document.getElementById('ProductPhotoImg');
  const thumbnailList = document.querySelector('.thumbnail-list');

  // Set first thumbnail as active by default
  if (thumbnails.length > 0) {
    thumbnails[0].classList.add('active');
  }

  // Image switcher functionality
  thumbnails.forEach(thumb => {
    thumb.addEventListener('click', function() {
      const newSrc = this.querySelector('img').getAttribute('data-full-url');
      
      // Animate main image switch
      mainImage.style.opacity = '0';
      mainImage.style.transform = 'scale(0.95)';
      
      setTimeout(() => {
        mainImage.src = newSrc;
        mainImage.style.opacity = '1';
        mainImage.style.transform = 'scale(1)';
      }, 200);
      
      // Update active state
      thumbnails.forEach(t => t.classList.remove('active'));
      this.classList.add('active');
    });
  });

  // Horizontal scroll with arrows
  let scrolling = false;
  const scrollStep = 200;

  thumbnailList.addEventListener('mousemove', (e) => {
    const rect = thumbnailList.getBoundingClientRect();
    const isLeftSide = e.clientX - rect.left < 50;
    const isRightSide = rect.right - e.clientX < 50;

    if (isLeftSide && thumbnailList.scrollLeft > 0) {
      thumbnailList.style.cursor = 'w-resize';
    } else if (isRightSide && thumbnailList.scrollLeft < thumbnailList.scrollWidth - rect.width) {
      thumbnailList.style.cursor = 'e-resize';
    } else {
      thumbnailList.style.cursor = 'default';
    }
  });

  thumbnailList.addEventListener('click', (e) => {
    const rect = thumbnailList.getBoundingClientRect();
    const isLeftSide = e.clientX - rect.left < 50;
    const isRightSide = rect.right - e.clientX < 50;

    if (isLeftSide) {
      thumbnailList.scrollBy({ left: -scrollStep, behavior: 'smooth' });
    } else if (isRightSide) {
      thumbnailList.scrollBy({ left: scrollStep, behavior: 'smooth' });
    }
  });
});
{% endjavascript %} 