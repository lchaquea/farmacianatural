{%- assign product = all_products[section.settings.product_handle] | default: product -%}

{% render 'hyperlab-global-styles' %}

<div class="hyperlab-section hyperlab-bg-white hyperlab-py-40">
  <div class="hyperlab-content">
    <div class="comparison-header">
      <h2 class="section-title">{{ section.settings.title | default: "¿Por qué elegir V10?" }}</h2>
      {% if section.settings.subtitle %}
        <p class="section-subtitle">{{ section.settings.subtitle }}</p>
      {% endif %}
    </div>

    <div class="comparison-grid">
      {% for block in section.blocks %}
        <div class="comparison-item">
          <div class="comparison-icon">{{ block.settings.icon }}</div>
          <h3 class="comparison-title">{{ block.settings.title }}</h3>
          <p class="comparison-text">{{ block.settings.description }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "V10 Comparison",
  "class": "hyperlab-section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "¿Por qué elegir V10?"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Section Subtitle"
    },
    {
      "type": "product",
      "id": "product_handle",
      "label": "Product"
    }
  ],
  "blocks": [
    {
      "type": "comparison_item",
      "name": "Comparison Item",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (emoji)",
          "default": "✨"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description"
        }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
.hyperlab-v10-comparison {
  padding: 5px;
  background: #fff;
}

.comparison-header {
  text-align: center;
  margin-bottom: 10px;
}

.section-title {
  font-size: 16px;
  font-weight: 700;
  margin-bottom: 8px;
  color: #1a1a1a;
}

.section-subtitle {
  font-size: 12px;
  color: #666;
  max-width: 400px;
  margin: 0 auto;
}

.comparison-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 8px;
  max-width: 800px;
  margin: 0 auto;
  padding: 8px;
}

.comparison-item {
  background: #f8f8f8;
  padding: 12px;
  border-radius: 6px;
  text-align: center;
}

.comparison-icon {
  font-size: 24px;
  margin-bottom: 8px;
}

.comparison-title {
  font-size: 14px;
  font-weight: 600;
  margin-bottom: 6px;
  color: #1a1a1a;
}

.comparison-text {
  font-size: 12px;
  color: #666;
  line-height: 1.3;
}

@media screen and (min-width: 768px) {
  .hyperlab-v10-comparison {
    padding: 10px;
  }

  .section-title {
    font-size: 18px;
  }

  .section-subtitle {
    font-size: 14px;
  }

  .comparison-grid {
    gap: 10px;
    padding: 12px;
  }

  .comparison-item {
    padding: 15px;
  }

  .comparison-icon {
    font-size: 28px;
    margin-bottom: 10px;
  }

  .comparison-title {
    font-size: 16px;
  }

  .comparison-text {
    font-size: 14px;
  }
}

@media screen and (min-width: 990px) {
  .hyperlab-v10-comparison {
    padding: 15px;
  }

  .section-title {
    font-size: 20px;
  }

  .section-subtitle {
    font-size: 16px;
  }

  .comparison-grid {
    gap: 12px;
    padding: 15px;
  }

  .comparison-item {
    padding: 18px;
  }

  .comparison-icon {
    font-size: 32px;
    margin-bottom: 12px;
  }

  .comparison-title {
    font-size: 18px;
  }

  .comparison-text {
    font-size: 16px;
  }
}
{% endstylesheet %} 